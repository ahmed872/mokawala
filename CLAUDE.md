# تعليمات إلزامية لأي جلسة عمل

**قبل أي شغل: اقرأ ملف `PROGRESS.md` في جذر الريبو.** هو سجل حالة المشروع — فيه إيه اللي خلص، إيه المعلق، وإزاي تجهز بيئة العمل والتحقق. البيئة هنا مؤقتة (ephemeral) وأي شغل غير مرفوع بيضيع، فالملف ده هو الذاكرة الوحيدة بين الجلسات.

**قواعد ثابتة:**

1. اقرأ `PROGRESS.md` أول الجلسة قبل ما تعيد استكشاف الكود من الصفر.
2. اعمل commit + push بعد كل جزء شغل مكتمل — متسيبش شغل غير مرفوع، الجلسة ممكن تقف في أي لحظة (حدود الاستخدام كل 5 ساعات).
3. حدّث `PROGRESS.md` مع كل push (قسم "الحالة الحالية" و"آخر شغل").
4. البرانش الحالي للشغل: راجع قسم Git في `PROGRESS.md`.
5. المشروع WPF ولا يُبنى على لينكس بالكامل — استخدم `tools/wpf_check.py` للتحقق (الشرح في PROGRESS.md).
6. التست `DriverAttendanceService_SavesWeeklyAttendance_...` فاشل من قبل أي تعديلات — مشكلة قديمة، متحاولش تصلحها ضمن شغل تاني.
7. لغة التواصل مع المستخدم (أحمد): العربية المصرية. نصوص الواجهة كلها عربية.
